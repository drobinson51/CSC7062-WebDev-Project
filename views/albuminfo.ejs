<!DOCTYPE html>
<html>
<head>
	<meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no">
	
	<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/purecss@3.0.0/build/pure-min.css"/>
	<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/purecss@3.0.0/build/grids-responsive-min.css" />
	<link rel="stylesheet" href="myui.css">
</head>

<body class = "dark">


<div class="pure-menu pure-menu-horizontal pure-menu-scrollable">
    <a href = "/" class="pure-menu-link pure-menu-heading">Stacks Of Wax</a>
    <ul class="pure-menu-list">
        <li class="pure-menu-item">
            <a href="/login" class="pure-menu-link">Sign-In</a>
       
        </li>
        <li class="pure-menu-item">
            <a href="/searchalbums" class="pure-menu-link">Search albums</a>
        </li>
		<li class="pure-menu-item">
            <a href="/searchuseralbums" class="pure-menu-link">Search user collections</a>
        </li>
        <li class="pure-menu-item">
            <a href="/home" class="pure-menu-link">Home</a>
        </li>
		</div>
        
<div class="pure-g centre">
    <div class="pure-u-1 pure-u-md-1-2-text">
	<h1>Welcome to Stacks of Wax</h1>
	<p>A curated collection of some of the finest Albums to have ever been.</p>
	</div> 


    <% let albumname = "" %>
    <% let upvotecount = "" %>
    <% let artist = "" %>
    <% let albumdesc  = "" %>
    <% let yearofrelease = "" %>
    <% let genre  = "" %>



    <% albumdata['data'].forEach( (album)=>{   %>

        <% if (albumname !== album.albumname){ %>
        <% albumname = album.album_title %>
        <% } %>
        <% if (artist !== album.artist){ %>
        <%  artist = album.artist %>
        <% } %>
        <% if (albumdesc !== album.album_desc){ %>
        <%  albumdesc = album.album_desc  %>
        <% } %>
        <% if (yearofrelease !== album.year_of_release){ %>
        <%  yearofrelease = album.year_of_release  %>
        <% } %>
        <% if (genre !== album.name){ %>
        <%  genre = album.name  %>
        <% } %>  
    <% }); %>
        
    <div class = "infocontainer">
    <div class = "infooutput">
    

    <header>
        <h2><%= albumname  %></h2>
      </header>
      <main>
        <p>
            <%= artist%>
        </p>
       
        <p>
                <%= albumdesc %>
        </p>

        <p>
           This album was released in: <%= yearofrelease %>
        </p>

        <p>
            This is a <%= genre %> album
         </p>

        <p>It contains the songs</p>
        <div class = "selectbar">
        <select name = "songinfo">
            <% albumdata['data'].forEach( (album)=>{   %>
       
         <option><%= album.title %>, length: <%= album.Time %> minutes</option>
        
        
        
         <% }); %>
            </select>
        </div>
      </main>

    </div>
    </div>
  
	</div>
</div>

</body>
</html>


   

               
            
       

    